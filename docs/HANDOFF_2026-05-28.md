# Handoff 2026-05-28 — Email Outreach (Технолайн Трейд)

## Что это
Холодная email-рассылка коммерческих предложений «Технолайн Трейд» по объектам
размещения Крыма (отели/пансионаты/базы/эллинги), с замыканием ответов в воронку.
Два слоя: **пилот** (работает сейчас) и **боевой модуль в CRM** (спроектирован, план готов).

## Бизнес-данные
- ООО «Технолайн Трейд», Симферополь. Гендиректор **Алексей Гуриненко**, +7 978 579-29-95.
- Профиль: поставка/снабжение — кондиционеры, бытовая/проф. техника, оборудование под спецификацию.
- Сайты: `simfer.com.ru` (лендинг), `climat-simf.ru` (техника, 8500+ моделей), `splithome.ru` (кондиционеры).

## Почта рассылки (настроена, проверена)
- Домен `simfer.com.ru` на **SprintHost**. Отправитель `alexey.gurinenko@simfer.com.ru`.
- SMTP `smtp.simfer.com.ru:465`, IMAP/POP `mail.simfer.com.ru`. Веб-почта — Roundcube (`webmail.sprinthost.ru`).
- **SPF/DKIM/DMARC настроены**, mail-tester **8.2/10**, не в блок-листах.
- Пульт — Gmail **`flycited2@gmail.com`**: «Отправлять как» от домена через SMTP SprintHost + переадресация ответов домена → этот Gmail. (Аккаунт `termoark@gmail.com` — НЕ пульт, был тестовым получателем.)
- DMARC-отчёты от `noreply-dmarc-support@google.com` — норма (не ошибка).

## Пилот — текущий режим работы
- **Google-таблица «Рассылка»** (id `1lnuzLwsDbEtM6TJySIYtLUAGmuXl494wN33Bm-TzR1U`, вкладка «Лист1»).
  Колонки: `Email, Название, Тип, Город, Телефон, Сайт, Соцсети, Адрес, Источник, Статус, Дата_отправки, Этап, Заметка`.
- **База контактов:** `data/outreach_contacts.csv` — **921 уникальный email** (валидировано, дедуп).
  Источник: парсер `result_enriched_*` (лист «ВСЕ», ~5748 строк; ~953 с email из CSV 0511 + дополнение из xlsx 0527).
- **Cowork (Gmail-коннектор)** делает ТОЛЬКО черновики (коннектор не умеет «Отправить»).
  Промпт: брать строки `Статус=новый`, шаблон вставлять дословно, НЕ слать, НЕ лезть в Chrome.
- **Отправка + статусы:** Apps Script `scripts/outreach_apps_script.gs` (v3, фикс таймаута: бюджет 4 мин).
  Меню «📧 Рассылка»: Отправить черновики (→ ставит «отправлено»+дата), Отметить ответивших,
  Пометить для фоллоу-апа, Обновить дашборд, Вкл/выкл авто-отправку (ежедневный триггер).
- **Прогрев:** 15 → 30 → 50 писем/день. Шаблон письма — финальный, в спеке (см. ниже).
- Текущий прогресс: первые ~20+ контактов уже отправлены (статус «отправлено», дата 2026-05-28).

## Боевой модуль в CRM (спроектирован)
- Репозиторий **`github.com/flycited2-dotcom/CRM_system`**: NestJS + Prisma + PostgreSQL 16 + Redis 7 + Next.js, Docker.
  VPS **`213.109.202.45`**, путь `/opt/crm`, ветка `codex/stage-0-1`. Стадии 0–3 готовы (auth, clients, leads).
- Модуль «Рассылки»: `OutreachContact` (холодная база отдельно от Clients), кампании, шаблоны, лог писем;
  отправка Nodemailer + очередь BullMQ; приём IMAP → **авто-создание Lead** (`source=email_outreach`) →
  существующий модуль лидов. Excel-подобный грид (AG Grid) + импорт/экспорт xlsx. Текст — шаблоны (Claude API в E5).
  Google-таблица/Apps Script выводятся из эксплуатации в боевой версии.
- Стадии E1–E5. **План E1 (Данные+грид+импорт/экспорт)** готов и детализирован (TDD, пути файлов, код).

## Ключевые документы
- Спека пилота: `docs/superpowers/specs/2026-05-26-email-outreach-pilot-design.md` (УТП, финальный шаблон письма).
- Спека CRM-модуля: `docs/superpowers/specs/2026-05-28-crm-email-outreach-stage-design.md`.
- План E1: `docs/superpowers/plans/2026-05-28-crm-email-outreach-e1.md`.
- Apps Script: `scripts/outreach_apps_script.gs`.
- База контактов: `data/outreach_contacts.csv`.

## Финальный шаблон письма (зафиксирован)
Приветствие: «Меня зовут Алексей Гуриненко, я руководитель компании «Технолайн Трейд»
(Симферополь). Мы помогаем объектам размещения в Крыму готовить номера и территорию к сезону.»
Далее «Чем именно можем быть полезны {Название}:» + 4 пункта (адаптируются по {Тип}) +
строка «если возникнет любой запрос — напишите, поможем, если в наших силах» + 3 ссылки +
строка-отписка + подпись гендиректора. УТП без 44-ФЗ/223-ФЗ (это для отдельной гос-кампании позже).

## Открытые задачи / следующий шаг
1. Заменить Apps Script в таблице на v3 (`scripts/outreach_apps_script.gs`) — фикс таймаута.
2. Продолжать пилот: черновики (Cowork) → отправка кнопкой → ответы помечать.
3. Стартовать **E1** модуля рассылок в отдельной ветке `feat/outreach-e1` (worktree) репозитория CRM —
   когда будет доступ к GitHub (на момент хендофа сеть до github.com падала).

## Безопасность / ограничения
- ⚠️ Пароль почты `alexey.gurinenko@simfer.com.ru` засветился в переписке — **рекомендована смена**. В файлах не хранится.
- В `data/` — персональные данные (email/телефоны): при инициализации git добавить `data/` в `.gitignore`.
- Apps Script (free Gmail): лимит выполнения 6 мин и ~100 писем/день — для пилота достаточно; в боевом модуле лимитов нет (BullMQ).
- Рабочая папка проекта пока НЕ под git.
